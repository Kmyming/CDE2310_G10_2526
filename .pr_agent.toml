[github_action_config]
# This block tells Qodo to run automatically on PR open
auto_review = false
auto_describe = false
auto_improve = false
pr_actions = ["update_changelog"]

[pr_update_changelog]
push_changelog_changes = true
changelog_file = "CHANGELOG.md"
extra_instructions = """
You are a senior robotics engineer. Analyze the PR diff and commit messages to update the changelog.

CRITICAL OUTPUT FORMAT:
You MUST output ONLY the new version block. 
NEVER output the `# Changelog` title. NEVER output the `Keep a Changelog` preamble text. 
Start exactly with `## [VERSION]` and STOP generating immediately after your last bullet point. 

## [VERSION] - YYYY-MM-DD
[High-level summary of the architectural, software, and hardware changes in this release.]

### Added / Changed
- feat(scope): [Explanation based on diff/commits]

### Fixed
- fix(scope): [Explanation based on diff/commits]

### Documentation
- docs(scope): [Explanation based on diff/commits]

### Testing
- test(scope): [Explanation based on diff/commits]

VERSIONING MATH:
Find the HIGHEST existing version number in the file. Use it as your baseline.
- MAJOR BUMP (X.0.0): Triggered ONLY if there is a `feat` AND the exact word "MAJOR" appears in the commit message/PR. 
- MINOR BUMP (0.X.0): Triggered if there is a `feat` (and no "MAJOR"). 
- PATCH BUMP (0.0.X): Triggered if there is a `fix`, `docs`, or `test`. 

THE BINARY BLINDSPOT:
If the code diff is empty or only shows binary files (.SLDPRT, .SLDASM, .STL), you MUST rely entirely on the PR Title and PR Description to understand the physical hardware changes. Log them as `feat(cad)` or `fix(cad)`. NEVER output empty bullet points; if a category has no changes, omit the header entirely.

ADDITIONAL CONSTRAINTS:
1. Replace [VERSION] with your calculated SemVer number. 
2. Replace YYYY-MM-DD with today's date. 
3. ONLY use these four conventional prefixes: feat, fix, docs, test.
4. Include the (scope) based on the specific ROS 2 package or module (e.g., architecture, launch, navigation).
5. Be highly specific about technical implementations, file names, and parameter changes.
6. Omit any empty category headers.
"""